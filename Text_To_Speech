import java.awt.Dimension;
import java.io.File;
import java.util.ArrayList;
import javax.sound.sampled.AudioSystem;
import javax.sound.sampled.Clip;
import javax.swing.JFrame;

public class Text_to_Speech_Class {

	private final static String[] numbers = {"zero","one", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten",
			"eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen", 
			"twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety",
			"hundred", "thousand", "million", "billion", "trillion", "point"};
	private final static String[] soundLocations = createSounds();
	private static Clip clip;
	
	private static String[] createSounds()
	{
		ArrayList <String> sounds = new ArrayList<String>();
		
		for(int i = 0; i < numbers.length; i++)
			sounds.add("C:\\eclipse proj\\GUICalculator\\sound\\" + numbers[i] + ".wav");
		
		return sounds.toArray(new String[sounds.size()]);
	}
	
	public static void main(String[] args) {
		
		JFrame f = new JFrame();
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		f.setSize(new Dimension(400,400));
		f.setVisible(true);
		
		ArrayList <String> words = new ArrayList<String>();
		
		String text = "two million three thousand one hundred thirty three point three six ";
		
		
		// finds all the words in the string and stores them in a list
		while(text.length() > 0)
		{
			words.add(text.substring(0, text.indexOf(' ')));
			text = text.substring(text.indexOf(' ') + 1, text.length());
		}
		
		Thread thread = new Thread() {
			@Override
    	    public void run(){
				for(String i : words)
				{
					for(int j = 0; j < numbers.length; j++)
					{
						if(i.contentEquals(numbers[j]))
						{
							try
							{
								clip = AudioSystem.getClip();
								clip.open(AudioSystem.getAudioInputStream(new File(soundLocations[j])));
							}
							catch (Exception e)
							{
								e.printStackTrace();
							}
							
							clip.start(); // play the sound
							
							
							try {
								Thread.sleep(700);
							} catch (InterruptedException e) {
								// TODO Auto-generated catch block
								e.printStackTrace();
							}
							break;
						}
					}
				}
			}
		};
		
		thread.start();
	}
}
